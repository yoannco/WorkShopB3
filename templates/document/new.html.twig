{% extends 'base.html.twig' %}

{% block title %}New Document{% endblock %}

{% block body %}
<div class="text-center">
    <h1>Ajouter un document</h1>

    {# {{ include('document/_form.html.twig') }} #}
     {{ form_start(form) }}
        <div class="form-group">
            {{ form_label(form.title, 'Titre du document (*) ', {'label_attr': {'class': 'font-weightbold'}}) }}
            {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="form-group text-center">
            {{ form_label(form.link, 'Document (*) ', {'label_attr': {'class': 'font-weightbold'}}) }}
            {{ form_widget(form.link, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="form-group mx-auto text-center">
            {{ form_widget(form.ajouter, { 'label': "Ajouter", 'attr': {'class': 'btn btn-primary mx-auto text-center'}}) }}
        </div>
    {{ form_end(form) }}

    <a href="{{ path('document_index') }}">Revenir à la liste</a>
</div>
{% endblock %}
